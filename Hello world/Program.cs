// but aita dile oh hobe na dile oh hobe, but use kora good habit 
using System;

// first of all we need a class
class Test {

    // then we need a main method
    // main method pass the parameter 
    // public keyword is used when we access into the main method value others method
    // void is used for return type, when we doesn't return any value then we used void otherwise we use return type datatype
    // system is a class
    // console is a member of the system
    // writeLine is a method of the system class
  public static void Main(string[] args ){
        Console.WriteLine(123);
        Console.WriteLine("Tamim khan");
    }
}
